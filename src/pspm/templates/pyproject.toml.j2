
[project]
name = "{{ name }}"
version = "0.1.0"
dependencies = []
requires-python = ">=3.8"
authors = []
description = "{{ description }}"
readme = "README.md"
classifiers = [
  "Programming Language :: Python"
]

{% if installable -%}
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
{% endif %}
